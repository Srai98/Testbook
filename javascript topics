
<!-- what is js, why it is fast?, asynchronous, synchronous, single threading, multiple threading, javascript engine, -->

where to include script tag in html file
output ways to show data using js in front end/browser
user defined,  pre-defined/inbuilt
keywords
if else
ternary operator ? :
typeof operator
operators in js

define javascript -
<!-- JavaScript is a dynamically typed language, which means that variable types are determined dynamically at runtime based on the values they hold -->